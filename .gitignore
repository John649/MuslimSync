.DS_Store
node_modules/
plugin/sourcemap.json
plugin/*.lock
plugin/src/AuthToken.luau
dist/
*.rbxm
*.rbxl
*.rbxlx

# Built on demand from assets/Logo.png, like the mac bundle in dist/.
assets/*.ico

# Vendored argon binaries are large; keep the one we ship for this platform.
!vendor/argon/**
